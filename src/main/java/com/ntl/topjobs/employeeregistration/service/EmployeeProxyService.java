package com.ntl.topjobs.employeeregistration.service;
/*package com.ntl.topjobs.employeesignuplogin.service;

import org.springframework.cloud.netflix.ribbon.RibbonClient;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

@FeignClient(name="employee-registration-service")
@RibbonClient(name="employee-registration-service")
public interface EmployeeProxyService {
	@GetMapping("/employer/login/emp/{id}/{password}")
	public boolean login(@PathVariable("id") String id, @PathVariable("password") String password);

}
*/